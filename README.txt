Program that takes 4 args:Bucket Count, Bucket size, Min value, and Max value

Creates Bucket Count "buckets" of size Bucket size with values between Min
value and Max Value

Individually sorts these buckets, once all the buckets are sorted it merges
them into one bucket of Bucket Count x Bucket size sorted numbers and outputs
these numbers to bucketList.out
